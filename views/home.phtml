<!-- Hero / About Section -->
<section id="about" class="section-hero">
    <div class="hero-bg-shapes">
        <div class="shape shape-1"></div>
        <div class="shape shape-2"></div>
        <div class="shape shape-3"></div>
    </div>

    <div class="container hero-container">
        <div class="hero-content">
            <span class="hero-greeting">Bienvenue dans mon univers</span>
            <h1 class="hero-title">Je suis <span
                    class="name-highlight"><?= htmlspecialchars($profile->full_name) ?></span></h1>

            <?php if (!empty($profile->description)): ?>
                <div class="hero-description">
                    <?= $profile->description ?>
                </div>
            <?php endif; ?>

            <div class="hero-actions">
                <a href="#projects" class="btn-primary hero-btn">Voir mes projets</a>
                <a href="#skills" class="btn-outline hero-btn">Mes compétences</a>
            </div>
</section>





<!-- Hero / About Section -->
<section id="about" class="section-about">
    <div class="container">
        <div class="profile-image">
            <img src="/assets/img/<?= $profile->image_url ?>" alt="<?= htmlspecialchars($profile->image_alt) ?>">
        </div>
        <h1>Bonjour, je suis <span class="name"><?= htmlspecialchars($profile->full_name) ?></span></h1>
        <?php if (!empty($profile->description)): ?>
            <div class="about-description">
                <?= $profile->description ?>
            </div>
        <?php endif; ?>
    </div>
</section>

<!-- Skills Section -->
<?php require_once __DIR__ . '/skills.phtml'; ?>

<!-- Projects Section -->
<section id="projects" class="section-projects">
    <div class="container">
        <h2>Mes Projets</h2>

        <!-- Filter bar -->
        <div class="project-filters">
            <button class="filter-btn active" data-filter="all">Tous</button>
            <?php foreach ($categories as $cat): ?>
                <button class="filter-btn" data-filter="<?= $cat->id ?>">
                    <?= htmlspecialchars($cat->name) ?>
                </button>
            <?php endforeach; ?>
        </div>

        <div class="projects-grid">
            <?php foreach ($projects as $project): ?>
                <div class="project-card" data-category="<?= $project->id_category ?>">
                    <?php if (!empty($project->image_url)): ?>
                        <div class="project-image">
                            <img src="/upload/<?= htmlspecialchars($project->image_url) ?>"
                                alt="<?= htmlspecialchars($project->image_alt ?? 'Projet image') ?>"
                                style="width: 100%; border-radius: 8px;">
                        </div>
                    <?php endif; ?>
                    <h3><?= htmlspecialchars($project->title) ?></h3>
                    <p><?= htmlspecialchars(substr($project->description, 0, 100)) ?>...</p>
                    <a href="/project?id=<?= $project->id ?>" class="btn-read-more">En savoir plus</a>
                </div>
            <?php endforeach; ?>
            <?php if (empty($projects)): ?>
                <p>Aucun projet à afficher pour le moment.</p>
            <?php endif; ?>
        </div>
    </div>
</section>